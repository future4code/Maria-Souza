export type User = {
   id: string
   name: string
   nickname: string
   email: string
   zipcode: string
   street: string
   number: string
   complement: string
   neighborhood: string
   city: string
   state: string
}

export type Address = {
   street: string,
   neighborhood: string,
   city: string,
   state: string,
}